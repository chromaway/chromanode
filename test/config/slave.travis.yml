chromanode:
  network: regtest
  enableHTTPS: false
  host: localhost
  port: 24446

logger:
  level: verbose # verbose, info, warning, error
  filename:

postgresql:
  url: postgres://postgres@localhost/travis_ci_test
  poolSize: 10
